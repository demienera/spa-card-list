import { CSSProperties } from "react";

export const useScreenshotFieldStyles = () => {
  return {
    space: {
      marginBottom: 8,
    } as CSSProperties,
    input: {
      width: 400,
    } as CSSProperties,
  };
};
