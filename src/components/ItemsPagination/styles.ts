import { CSSProperties } from "react";

export const usePaginationStyles = () => {
  return {
    conteiner: {
      textAlign: "center",
      marginTop: 48,
      marginBottom: 32,
      padding: "24px 0",
    } as CSSProperties,
  };
};
